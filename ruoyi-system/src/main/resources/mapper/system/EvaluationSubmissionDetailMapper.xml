<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.EvaluationSubmissionDetailMapper">

    <resultMap id="EvaluationSubmissionDetailResult" type="com.ruoyi.system.domain.EvaluationSubmissionDetail">
        <id property="id" column="id" />
        <result property="submissionId" column="submission_id" />
        <result property="ruleId" column="rule_id" />
        <result property="ruleSnapshot" column="rule_snapshot" />
        <result property="finalScore" column="final_score" />
        <result property="status" column="status" />
        <result property="remark" column="remark" />
        <result property="auditTime" column="audit_time" />
        <result property="auditBy" column="auditor_id" />
        <result property="auditComment" column="audit_comment" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <delete id="deleteBySubmissionId" parameterType="long">
        delete from evaluation_submission_detail where submission_id = #{submissionId}
    </delete>

    <insert id="insertEvaluationSubmissionDetail" parameterType="com.ruoyi.system.domain.EvaluationSubmissionDetail" useGeneratedKeys="true" keyProperty="id">
        insert into evaluation_submission_detail (
            submission_id,
            rule_id,
            rule_snapshot,
            final_score,
            status,
            remark,
            audit_time,
            auditor_id,
            audit_comment,
            create_by,
            create_time,
            update_by,
            update_time
        ) values (
            #{submissionId},
            #{ruleId},
            #{ruleSnapshot},
            #{finalScore},
            #{status},
            #{remark},
            #{auditTime},
            #{auditBy},
            #{auditComment},
            #{createBy},
            #{createTime},
            #{updateBy},
            #{updateTime}
        )
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        insert into evaluation_submission_detail (
            submission_id,
            rule_id,
            rule_snapshot,
            final_score,
            status,
            remark,
            audit_time,
            auditor_id,
            audit_comment,
            create_by,
            create_time,
            update_by,
            update_time
        ) values
        <foreach collection="list" item="d" separator=",">
            (
                #{d.submissionId},
                #{d.ruleId},
                #{d.ruleSnapshot},
                #{d.finalScore},
                #{d.status},
                #{d.remark},
                #{d.auditTime},
                #{d.auditBy},
                #{d.auditComment},
                #{d.createBy},
                #{d.createTime},
                #{d.updateBy},
                #{d.updateTime}
            )
        </foreach>
    </insert>

</mapper>