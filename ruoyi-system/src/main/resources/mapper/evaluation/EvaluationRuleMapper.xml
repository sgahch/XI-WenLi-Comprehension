<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.evaluation.mapper.EvaluationRuleMapper">

    <!-- 根据维度查询规则列表 -->
    <select id="selectRulesByDimension" parameterType="map" resultType="map">
        SELECT 
            id,
            dimension,
            college_id,
            category,
            item_name,
            level,
            score,
            description,
            require_attachment,
            attachment_types,
            is_enabled
        FROM evaluation_rule
        WHERE dimension = #{dimension}
          AND is_enabled = 1
        ORDER BY category, item_name, score DESC
    </select>

</mapper>

