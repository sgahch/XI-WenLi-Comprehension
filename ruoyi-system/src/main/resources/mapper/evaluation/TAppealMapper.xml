<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.TAppealMapper">
    
    <resultMap type="TAppeal" id="TAppealResult">
        <result property="id"    column="id"    />
        <result property="recordId"    column="record_id"    />
        <result property="studentId"    column="student_id"    />
        <result property="appealReason"    column="appeal_reason"    />
        <result property="status"    column="status"    />
        <result property="processResult"    column="process_result"    />
        <result property="processBy"    column="process_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="processTime"    column="process_time"    />
    </resultMap>

    <sql id="selectTAppealVo">
        select id, record_id, student_id, appeal_reason, status, process_result, process_by, create_time, process_time from t_appeal
    </sql>

    <select id="selectTAppealList" parameterType="TAppeal" resultMap="TAppealResult">
        <include refid="selectTAppealVo"/>
        <where>  
            <if test="recordId != null "> and record_id = #{recordId}</if>
            <if test="studentId != null "> and student_id = #{studentId}</if>
            <if test="appealReason != null  and appealReason != ''"> and appeal_reason = #{appealReason}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="processResult != null  and processResult != ''"> and process_result = #{processResult}</if>
            <if test="processBy != null "> and process_by = #{processBy}</if>
            <if test="processTime != null "> and process_time = #{processTime}</if>
        </where>
    </select>
    
    <select id="selectTAppealById" parameterType="Long" resultMap="TAppealResult">
        <include refid="selectTAppealVo"/>
        where id = #{id}
    </select>

    <insert id="insertTAppeal" parameterType="TAppeal" useGeneratedKeys="true" keyProperty="id">
        insert into t_appeal
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="recordId != null">record_id,</if>
            <if test="studentId != null">student_id,</if>
            <if test="appealReason != null and appealReason != ''">appeal_reason,</if>
            <if test="status != null">status,</if>
            <if test="processResult != null">process_result,</if>
            <if test="processBy != null">process_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="processTime != null">process_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="recordId != null">#{recordId},</if>
            <if test="studentId != null">#{studentId},</if>
            <if test="appealReason != null and appealReason != ''">#{appealReason},</if>
            <if test="status != null">#{status},</if>
            <if test="processResult != null">#{processResult},</if>
            <if test="processBy != null">#{processBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="processTime != null">#{processTime},</if>
         </trim>
    </insert>

    <update id="updateTAppeal" parameterType="TAppeal">
        update t_appeal
        <trim prefix="SET" suffixOverrides=",">
            <if test="recordId != null">record_id = #{recordId},</if>
            <if test="studentId != null">student_id = #{studentId},</if>
            <if test="appealReason != null and appealReason != ''">appeal_reason = #{appealReason},</if>
            <if test="status != null">status = #{status},</if>
            <if test="processResult != null">process_result = #{processResult},</if>
            <if test="processBy != null">process_by = #{processBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="processTime != null">process_time = #{processTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteTAppealById" parameterType="Long">
        delete from t_appeal where id = #{id}
    </delete>

    <delete id="deleteTAppealByIds" parameterType="String">
        delete from t_appeal where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>